SELECT *,
CASE 
    WHEN (x+y)>z AND (x+z)>y AND (y+z)>x THEN 'Yes' 
    ELSE 'No'
END AS triangle
FROM triangle

#2 sides plus together is bigger than 3rd to form a triangle
#structure: CASE
                WHEN
            END AS
            FROM 
